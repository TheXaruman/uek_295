# ===================================================================
#                Example Environment Variables
# ===================================================================
#
# This file provides a template for the environment variables used by the application.
# For local development, copy this file to a new file named '.env'
# and replace the placeholder values with your actual configuration.
#
# IMPORTANT: The '.env' file should NEVER be committed to version control.
#
# ===================================================================


# --- Application Settings ---
# -------------------------------------------------------------------
# The port the NestJS server will listen on.
PORT=3000

# The global prefix for all API routes (e.g., '/api/v1').
# If you change this, update the reverse proxy or client-side accordingly.
GLOBAL_PREFIX=api


# --- Security & JWT Authentication ---
# -------------------------------------------------------------------
# This is a critical secret used to sign and verify JSON Web Tokens.
# It MUST be a long, random, and unpredictable string.
#
# !! DO NOT USE THIS DEFAULT VALUE IN PRODUCTION !!
#
# Generate a secure key with a command like:
# > node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=some-default-secret-key-that-should-be-changed

# Defines the expiration time for JWTs.
# Format is a string describing a time span (e.g., "1d", "15m", "2h", "7d").
JWT_EXPIRES_IN=1d


# --- Database Configuration ---
# -------------------------------------------------------------------
# This application is configured to use SQLite.
# The path is relative to the project root.
DB_DATABASE=data/todo.db

# The following variables are examples for a PostgreSQL setup and are not
# currently used. They are here to guide future database migrations.
#
# DB_TYPE=postgres
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=myuser
# DB_PASSWORD=mypassword
# DB_SYNCHRONIZE=true # Should be 'false' in production to prevent data loss.


# --- API Documentation (Swagger) ---
# -------------------------------------------------------------------
# The path where the Swagger/OpenAPI documentation UI will be available.
# Example: http://localhost:3000/doc
SWAGGER_DOC_PATH=doc
